// A tuple is an array which store multiple fields belong to different data types.
// It is similar to the structure in the C programming language.

// A tuple is a datatype represents the heterogenous collection of values and can also be passed as parameters in a function call.
//Syntax - let tuple_name = [val1,val2,val3,val4,valn];
let arrTuple = [ 101, "Kiran", true];
console.log(arrTuple);
arrTuple.push(102);





